# MikroTik syslog receiver
# Receive UDP syslog from MikroTik CRS309 on port 1514
module(load="imudp")
input(type="imudp" port="1514")

# Write MikroTik logs to dedicated file
if $fromhost-ip == '192.168.10.1' then {
    action(type="omfile" file="/var/log/mikrotik.log")
    stop
}
